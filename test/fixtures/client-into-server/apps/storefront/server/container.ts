// Fixture. Stands in for the composition root: it holds the database handle and the
// four secrets, and no browser bundle may ever contain it.
export const connectionString = 'postgresql://talla_app:fixture@localhost:5432/talla';
