{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["node"]
  },
  "include": ["test/**/*.ts", "*.config.ts", "*.config.js"],
  // The planted violation is input to the boundary gate, not code that has to compile.
  "exclude": ["test/fixtures/**"]
}
