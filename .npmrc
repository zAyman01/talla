# Hoisting hides a missing dependency until it breaks in CI or in a worker image.
# Keep the strict default explicit so nobody relaxes it by accident.
node-linker=isolated
strict-peer-dependencies=true
auto-install-peers=false
